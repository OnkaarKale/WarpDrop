services:
  - type: web
    name: warpdrop
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
